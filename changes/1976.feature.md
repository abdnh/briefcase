A new `standalone` Linux output format produces a portable `.zip` bundle that embeds a `python-build-standalone` Python interpreter alongside the application, decoupling the resulting artefact from the host distribution's system Python.
