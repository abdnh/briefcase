Linux system packages (`.deb`, `.rpm`, `.pkg.tar.zst`) can now embed a standalone Python interpreter via the new `embed_python` configuration option, decoupling the produced package from the distribution's default Python.
